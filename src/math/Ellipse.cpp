#include "Ellipse.h"

namespace jvgs
{
    namespace math
    {
        Ellipse::Ellipse(Vector2D radius)
        {
            this->radius = radius;
        }

        Ellipse::~Ellipse()
        {
        }
    }
}
