#include "Ellipse.h"

namespace jvgs
{
    namespace math
    {
        Ellipse::Ellipse(float x, float y): Vector2D(x, y)
        {
        }

        Ellipse::~Ellipse()
        {
        }
    }
}
