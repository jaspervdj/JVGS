#include "Positioner.h"
#include "Entity.h"

namespace jvgs
{
    namespace game
    {
        Positioner::Positioner(Entity *entity): Affector(entity)
        {
        }

        Positioner::~Positioner()
        {
        }
    }
}
